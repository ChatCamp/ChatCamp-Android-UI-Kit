<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chatcamp.uikit">
    <application
        android:allowBackup="true"
        android:supportsRtl="true">
        <activity android:name="com.chatcamp.uikit.preview.MediaPreviewActivity" />

        <provider
            android:name="com.chatcamp.uikit.utils.ChatCampFileProvider"
            android:authorities="${applicationId}.chatcamp.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/library_file_paths" />
        </provider>

        <activity
            android:name="com.chatcamp.uikit.preview.ShowImageActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.chatcamp.uikit.preview.ShowVideoActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.chatcamp.uikit.conversationdetails.GroupDetailActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.chatcamp.uikit.conversationdetails.UserProfileActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <service android:name=".messages.DownloadVideoIntentService"></service>
    </application>

</manifest>